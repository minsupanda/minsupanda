[최대값 최소값 문제]

<string>

string을 sort로 정렬하면 '-' , ' ' , 숫자 다 하나씩 전부 정렬된다 (숫자도 크기에따라 정렬이 된다)
stoi 할 때 -도 인식할 수 있어서 + 던가 - 던가 다 제대로 저장이 된다

string을 for( : ) 로 돌리면 char형으로 인식되고, 이 값을 저장하게 되면 아스키 코드값으로 저장되서 원하는 값이 저장이 안 된다.]
-> 계속 40몇 50 몇 이런 숫자가 들어갔던 이유

아스키 코드값 내의 숫자는 문자 그대로의 숫자를 받고 싶을 때 '0'을 빼면 되고 - 값일 경우는 '0을 더하면 된다
 -> 다만 아스키 코드 값의 범위를 넘어서는 숫자는 당연이 안 됨(500을 넣었을 때 제대로 안되었던 이유);

https://jhnyang.tistory.com/107(getline 관련 자료) : getline으로 문자 받아서 문제 해결 가능하다
-> #include<string>이 필요함