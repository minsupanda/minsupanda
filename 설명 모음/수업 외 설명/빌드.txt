[Build - 빌드]
- 빌드 단축키는 [컨트롤 + 쉬프트 + B] or [F5]
- 빌드를 하는 것은 실행파일(.exe)을 생성하는 것이다
- 만약 자신이 리소스 파일을 추가적으로 생성해서 만들었다면 그 파일은 필수적으로 .exe파일이 있는 경로에 함께 있어야 한다.
- 32bit면 Debug 파일에, 64bit면 64라고 따로 파일이 생성되는에 그 곳에 .exe파일이 생성된다.