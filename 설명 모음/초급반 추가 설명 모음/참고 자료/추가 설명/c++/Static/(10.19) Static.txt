<전역변수>
- 장점 : 프로그램이 실행될 때 데이터가 만들어지고 종료되야 사라지기때문에 안정성이 좋다
- 단점 : 커플링이 높기 때문에 객체지향과 어울리지 않다.

<Static>
- 장점 : 전역변수의 장점만 가지고 온 것이다.
- 지역변수처럼 선언된 지역안에서만 사용이 가능하다.
- 많이 쓰는 문법이니 잘 기억하기
- 같이쓰는 변수, 함수

ex.
class Monster
멤버변수
공격력, 공격속도, 이동속도
static 함수
총 몬스터 수를 관리하고 싶을 때
각자 몬스터 클래스 생성자에 ++을 넣고, 소멸자에 --를 넣으면서 관리 가능하다

둘다 Date영역에서 관리되는 코드들이다.


string str;
string* str1 = new string;
*str1 = str;

cout << str;
cout << *str;

string은 배열, 즉 주소를 사용하는 체계가 아니기 때문에 포인터를 사용해줘야한다.
(안에서 배열을 관리하는 기능을 하지 string자체는 주소를 가지지 않기때문)

char st[256];
char* st1 = new char[256]

cout << str;
cout << str;

char은 애당초 얘가 시작주소를 가지고 주소체계로 접근해 출력을 하는 애(NULL을 만날때 까지 출력)
그렇기 때문에 여기서 한번더 *를 붙여 출력을 시킨다면 시작주소를 따라가는 것이기 때문에 문자 하나만 출력이 되는 것