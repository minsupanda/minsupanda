#pragma warning(disable:4996)
#include <stdio.h>

void main()
{
	int num1, num2, sum;
	printf("두 정수를 입력하시오 : ");
	scanf("%d%d", &num1, &num2); // num 변수의 메모리 주소에 직접 접근해서(&의 기능) 값을 집어넣는다
	sum = num1 + num2;
	printf("%d + %d = %d", num1, num2, sum);
}